import React from "react";

const FilterSideBar = () => {
      return (
            <div className="md:block hidden w-[30%] h-screen bg-violet-500">
                  FilterSideBar
            </div>
      );
};

export default FilterSideBar;
